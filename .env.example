# Copy to .env.local and fill in real values to make the IMAP-backed
# SwiftUI previews fetch live mail. .env.local is gitignored.
# One numbered pair per mailbox; server and port come from the domain.

email1=
password1=

email2=
password2=
